import 'package:flutter/material.dart';

class ArticleViewScreenUI extends StatelessWidget {
  const ArticleViewScreenUI({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Name"),
      ),
      body: Container(),
    );
  }
}
